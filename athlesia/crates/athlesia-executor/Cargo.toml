[package]
name = "athlesia-executor"
version = "0.1.0"
edition = "2021"
[dependencies]
athlesia-types = { path = "../athlesia-types" }
[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
